import { render } from '@testing-library/svelte';
import App from '@/App.svelte';

describe('App ...', () => {
  it.todo('should render termnial entry given by terminal input', () => {
    render(App, { terminalInitialized: true });
  });
});
